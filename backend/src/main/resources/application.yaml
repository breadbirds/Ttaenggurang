spring:
  profiles:
    active: prod
  application:
    name: ttaenggrang
  datasource:
    url: jdbc:mysql://localhost:3307/ttgr?useSSL=false&allowPublicKeyRetrieval=true&useUnicode=true&serverTimezone=Asia/Seoul
    username: root
    password: ssafy
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql:
        true
  sql:
    init:
      mode: always  # 서버 실행 시 init.sql 실행
  springdoc: # Swagger
    swagger-ui:
      groups-order: DESC # path, query, body, response 순으로 출력
      tags-sorter: alpha # 태그를 알파벳 순으로 정렬
      operations-sorter: method  # method: delete - get - patch - post - put 순으로 정렬, alpha: 알파벳 순으로 정렬 가능
    paths-to-match:
      - /api/** # swagger-ui에 표시할 api의 엔드포인트 패턴
    api-docs:
      path: /api/v3/api-docs  # ✅ OpenAPI 문서 경로를 /api로 설정

jwt:
  secret: "dF8hX29jVnMbQ3pA6xYzK8eU4pTfH2vN"  # ✅ 반드시 안전한 키로 설정

server:
  servlet:
    context-path: /api
    encoding:
      charset: UTF-8
      enabled: true
      force: true